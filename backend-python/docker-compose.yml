services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dataplate
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: dataplate_local
    ports:
      - "5433:5432"
    volumes:
      - dataplate_postgres_data:/var/lib/postgresql/data
      - ./db/init/00_extensions.sql:/docker-entrypoint-initdb.d/00_extensions.sql:ro
      - ../database/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d dataplate"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: .
    environment:
      DB_HOST: db
      DB_PORT: 5432
      PORT: 8081
      DB_NAME: dataplate
      DB_USER: postgres
      DB_PASSWORD: dataplate_local
      JWT_SECRET: DataPlateJwtSecretKeyForDevelopmentOnlyChangeMe123456789
      JWT_EXPIRATION_MS: 86400000
      JWT_REFRESH_EXPIRATION_MS: 604800000
      ALLOWED_ORIGINS: http://localhost:5500,http://127.0.0.1:5500,http://localhost:8081,http://127.0.0.1:8081,http://localhost:5173,http://127.0.0.1:5173
    ports:
      - "8081:8081"
    depends_on:
      db:
        condition: service_healthy

  adminer:
    image: adminer:4
    environment:
      ADMINER_DEFAULT_SERVER: db
    ports:
      - "8082:8080"
    depends_on:
      db:
        condition: service_healthy

volumes:
  dataplate_postgres_data:
